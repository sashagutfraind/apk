This folder is not in version control.  The complete model is built from a local Eclipse 
installation and the scrips are managed in the local git repo instead of on bebop, so 
make sure any changes here are copied to a local repo and saved.
